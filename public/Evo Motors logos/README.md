# Evo Motors logo assets

- **logo.svg** – Main logo (references `logo-1024.png` in this folder).
- Place **logo-1024.png** (1024×1024) here if the SVG does not display.
- The site header and footer use the path `/Evo Motors logos/logo.svg` from `lib/site-config.ts`.
